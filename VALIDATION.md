# 验证记录

验证日期：2026-07-21  
工具链：Xcode 26.6（Build 17F113）、Swift 6.3.3、iOS Simulator SDK 26.5

## 结果

- `xcodebuild -list`：成功识别 `FitTune`、`FitTuneTests` 和共享 Scheme。
- iOS Swift 全量类型检查（arm64、iOS 17 Simulator target）：通过。
- `FitTune` App Target 编译、链接、Info.plist 生成、隐私清单复制和 Bundle 校验：通过。
- `FitTuneTests` Xcode Target 编译和链接：通过。
- Swift Package 核心规则测试：26 项全部通过，0 失败。
- iPhone 17 Pro（iOS 26.5）Simulator XCTest（关闭并行克隆）：11 项全部通过，0 失败，`** TEST SUCCEEDED **`。
- iPhone 17 Pro（iOS 26.5）人工 UI 冒烟测试：通过。

测试覆盖：

1. 良好恢复状态保持计划。
2. 低恢复状态降低负荷并减组。
3. 达到次数上限且 RIR 充足时小幅加重。
4. 未达到目标次数时降低重量。
5. 恢复训练的起始训练量低于高级增肌计划。
6. 计划遵守每周天数和器械选择。
7. e1RM 估算包含 RIR 并限制高次数误差。
8. 同动作完成质量良好且恢复约 72 小时后，下一次建议重量小幅上调。
9. 同动作模式训练不足 24 小时时，下一次建议重量保守下调。
10. 用户选定的三分化能按周训练日生成，力量与有氧模块分别输出。
11. 器械替换只提供相同动作模式的候选动作。
12. 可独立生成仅针对胸部的今日训练。
13. 避开部位后，会移除所有涉及该部位的动作模式。
14. 主训练部位与避开部位冲突时，不会生成有风险的训练任务。
15. 部分完成记录会参与训练历史与负重学习，但不会推进完整计划轮转。
16. Mifflin–St Jeor 静息能量计算与缺失参数保护。
17. 有氧净 MET 消耗计算及手表实测热量优先级。
18. 连续极限/动作质量下降时，动态延长组间休息、降低重量并停止动作。
19. 扩充后的动作库覆盖主要动作模式、器械与替代方案。

## 模拟器人工验收

已在本机安装 iOS 26.5.1 与 iOS 26.5 Simulator，并在 iPhone 17 Pro 模拟器完成以下流程：

1. 首次启动与三步引导：目标、经验、训练条件。
2. 自动生成身体重组 3 日计划。
3. 启动训练，记录 12 组并显示逐组 RIR/重量建议。
4. 打开疼痛开关后，继续按钮被禁用并显示停止训练与就医提醒；关闭后可继续。
5. 完成训练后，历史、累计组数、下一次训练与 e1RM 自动更新。
6. 新增手动体重记录并显示体重趋势。
7. 将每周训练从 3 天改为 4 天，确认后成功重建为上下肢四日计划，历史记录保留。

## v0.2 增量人工验收

在保留 v0.1 本地快照的情况下直接安装 v0.2，确认旧数据可以解码并继续使用：

1. “今天”页新增睡眠质量输入，并保留原有睡眠时长、酸痛、压力和训练意愿。
2. 力量计划能根据既往记录展示首组建议重量；实测显示 `17.5 kg`、`10.0 kg` 与“待校准”三种状态。
3. “计划”页逐动作展示建议重量、置信度和生成原因，包括不足 24 小时的保守调整说明。
4. 力量和有氧显示为两个独立模块，不再把体能动作混入力量训练。
5. 器械替换菜单可打开，并只列出同动作模式候选；实测将杠铃卧推替换为哑铃卧推后，计划即时更新且历史不丢失。
6. 设置页能独立选择力量目标、有氧目标、训练分化、每周力量天数和器械条件。
7. 进展页已加入动作级 e1RM、12 分钟距离/VO₂max/静息心率记录，以及睡眠和恢复趋势。

旧快照中的计划本身不会被静默改写；用户点击“保存并重新生成计划”后才按 v0.2 规则生成新的力量/有氧安排，从而避免升级时意外覆盖既有训练计划。

## v0.2 真机验收

- 设备：iPhone 15 Pro Max（iOS 26.5，iPhone16,2）。
- 签名：Apple Development 与 `com.codex.fittune` 团队配置文件验证通过。
- 安装：同 Bundle ID 覆盖安装成功，设备端识别版本 `0.2.0`、build `2`。
- 启动：通过 CoreDevice 启动成功，并确认设备上 FitTune 进程持续运行。
- 迁移：v0.1 本地快照升级后可正常打开；训练历史和计划保留。
- 界面：真机截图确认新增睡眠质量输入、84 分恢复状态、力量建议重量/待校准状态均正常显示，无首屏截断或启动崩溃。
- 截图：`Screenshots/FitTune-v0.2-iPhone15ProMax.png`（1290 × 2796）。

首次由 Xcode 直接 Run 时，调试器在模拟器冷启动阶段曾长时间停在黑色启动画面；停止调试后从模拟器主屏直接打开 App 即正常显示。应用本身无启动崩溃，后续 XCTest 也在同一 Runtime 上完整通过。

## v0.3 增量人工验收

在保留 v0.2 本地快照的情况下安装 v0.3，旧训练历史与个性化计划均可继续读取。已在 iPhone 17 Pro（iOS 26.5）Simulator 完成以下流程：

1. “今天”页不再自动认定必须训练，可明确选择“今天训练”或“今天休息”。
2. 选择“胸”后，今日卡片即时生成“胸部训练”，不再绑定原计划中的下肢训练。
3. 将“肩”设为今日避开部位后，肩部按钮被禁用，涉及肩部的卧推动作从胸部训练中移除，并显示“今天避开肩”。
4. 训练中打开“更换当前动作或器械”，可按哑铃、固定器械/绳索、徒手/弹力带分组选择；实测将蝴蝶机夹胸切换为哑铃飞鸟。
5. 完成 1 组后点击结束，出现“保存 1 组并结束 / 放弃且不保存 / 继续训练”三种选择。
6. 保存后首页历史新增“胸部训练 · 部分完成 · 1 组”，累计有效组数从 12 增至 13，并据该组更新下次建议重量。
7. 单元测试确认部分完成记录不会推进下一节计划轮转。

- 模拟器截图：`Screenshots/FitTune-v0.3-daily-choice-simulator.jpeg`、`Screenshots/FitTune-v0.3-partial-workout-simulator.jpeg`。
- App 版本：`0.3.0`，build `3`。

## v0.3 真机验收

- 设备：iPhone 15 Pro Max（iPhone16,2），CoreDevice 连接正常。
- 签名：Apple Development 与 `com.codex.fittune` 团队配置文件验证通过。
- 构建：iPhoneOS 26.5 SDK arm64 构建、签名、Bundle 校验均通过，`** BUILD SUCCEEDED **`。
- 安装：使用同 Bundle ID 覆盖安装成功；设备端确认版本 `0.3.0`、build `3`，原 App 数据容器未被主动删除。
- 启动：CoreDevice 返回启动成功，随后确认 FitTune 真机进程持续运行（PID 3997）。

## v0.4 增量人工验收

在保留 v0.3 本地快照的情况下安装 v0.4，旧训练历史、计划和体重记录均可继续读取。已在 iPhone 17 Pro（iOS 26.5）Simulator 完成以下流程：

1. “今天”页显示“今日热量消耗”，拆分静息基础消耗、力量、有氧和 Watch 其他活动；基础参数不完整时明确提示补全，不猜测数值。
2. 手动记录 30 分钟 Zone 2 跑步后，按净 MET 估算 268 千卡；首页同步显示跑步时长、有氧效果 60/100 和预计恢复 12 小时。
3. 有氧方式菜单包含跑步、爬坡走、爬楼机、游泳、骑行、划船机、椭圆机、快走和跳绳，可录入时长、距离、平均心率及手表实测热量。
4. 训练中可从扩展动作库增加动作，也可减少当前动作；实测加入前蹲后动作数从 3 增至 4，再移除回到 3。
5. 训练中可增减本动作计划组数；实测从 2 组调整为 3 组。
6. 每组结束均询问主观感受；选择“吃力”后建议休息 3 分钟，并限制剩余组数。
7. 连续两组选择“接近极限 / 状态差”，同时 RIR 为 0、动作质量为 2 时，建议重量从 20 kg 降至 15 kg、休息延长至 4 分钟，并显示“建议结束本动作”。
8. 提前结束并保存 3 组后，训练被标记为部分完成，并显示力量刺激 60、增肌刺激 45、疲劳 51、预计恢复 36 小时和主动消耗约 22 千卡。
9. 进展页的体重、恢复、主力动作 e1RM、有氧时长与次数均限制为最近 30 天趋势。
10. HealthKit 同步入口可读取当日 activeEnergyBurned 与 HKWorkout；手表实测热量覆盖 MET 估算，并通过外部 UUID 去重。

- 模拟器截图：`Screenshots/FitTune-v0.4-energy-cardio-simulator.jpeg`、`Screenshots/FitTune-v0.4-dynamic-rest-simulator.jpeg`、`Screenshots/FitTune-v0.4-stop-warning-simulator.jpeg`、`Screenshots/FitTune-v0.4-workout-effect-simulator.jpeg`。
- App 版本：`0.4.0`，build `4`。

## v0.4 真机验收

- 设备：iPhone 15 Pro Max（iPhone16,2），CoreDevice 连接正常。
- 构建：iPhoneOS 26.5 SDK arm64 构建、签名、Bundle 校验均通过，`** BUILD SUCCEEDED **`。
- 签名：Apple Development 与 `com.codex.fittune` 团队配置文件验证通过。
- 安装：使用同 Bundle ID 覆盖安装成功，未卸载应用或主动清除原数据容器。
- 版本：设备端确认 `0.4.0`、build `4`。
- 启动：CoreDevice 返回启动成功，并确认 FitTune 真机进程持续运行（PID 4571）。

## v0.5 增量人工验收

在保留 v0.4 本地快照的情况下安装 v0.5，旧力量、有氧、体重、恢复与计划数据均可继续读取。已在 iPhone 17 Pro（iOS 26.5）Simulator 验证：

1. 今日能量卡新增步行消耗、今日步数和其他活动，并维持静息/力量/有氧分项。
2. 有氧时长、距离、速度、坡度、平均心率、session-RPE 和设备能量均显示为可直接编辑文本框，同时保留加减按钮。
3. 爬坡走直接键入 6.0 km/h、10% 坡度、30 分钟后，ACSM 步行速度/坡度公式输出约 294 kcal、合理区间 250–338 kcal、中高置信度。
4. 无速度/心率/设备能量时回退至 2024 Compendium MET，并明确显示低至中置信度。
5. 力量训练的计划组数、重量、次数、RIR、平均心率和设备主动能量均可直接键入；实测计划组数从 2 改为 1 后即时生效。
6. 训练后感受细化为 RPE 5–6、7、8、9、9.5、10、动作变形和疼痛/异常不适八类。
7. 完成动作全部组后显示勾选动画、“动作完成”和下一动作名称，并提供“开始下一个动作”。
8. “我的”新增体脂率、去脂体重、腰围、实测 RMR、静息心率和实测最大心率，且均支持直接输入。
9. 记录管理可分别删除力量、有氧和体重记录；实测删除一次 3 组力量训练后回收站从 0 变为 1，点击恢复后重新变为 0，原记录恢复。
10. 动作库按肌群与杠铃、哑铃、史密斯、固定器械、绳索、徒手/弹力带分组，扩充胸、肩、背和腿部候选动作。
11. HealthKit 同步读取步数、步行距离、主动能量、运动平均心率；手动估算记录可按类型、日期和时长匹配为 Watch 实测记录，力量训练同样支持回填。

新增自动化覆盖：Cunningham、实测 RMR 优先级、速度/坡度方程、Keytel 心率模型、力量设备能量覆盖、恢复范围/session-RPE 负荷和具体器械分类。Swift Package 共 26 项测试，0 失败。

- 模拟器截图：`Screenshots/FitTune-v0.5-tiered-cardio-energy-simulator.jpeg`、`Screenshots/FitTune-v0.5-exercise-completion-simulator.jpeg`、`Screenshots/FitTune-v0.5-recoverable-records-simulator.jpeg`。
- App 版本：`0.5.0`，build `5`。

## v0.5 真机验收

- 设备：iPhone 15 Pro Max（iPhone16,2），CoreDevice 连接正常。
- 构建：iPhoneOS 26.5 SDK arm64 构建、签名与 Bundle 校验均通过，`** BUILD SUCCEEDED **`。
- 签名：Apple Development 与 `com.codex.fittune` 团队配置文件验证通过。
- 安装：以同一 Bundle ID 覆盖安装成功；保留 v0.4 的应用数据与训练记录，未卸载应用或清除数据容器。
- 版本：设备端确认 `0.5.0`、build `5`。
- 启动：第一次因手机处于锁定状态被系统拒绝；解锁后重试成功，并确认 FitTune 真机进程持续运行（PID 4911）。

## v0.6 增量验收

自动化覆盖 42 项测试，0 失败；使用 iPhone 17 Pro（iOS 26.5）Simulator SDK 构建通过。新增重点：

1. 训练草稿可完整编码/解码，重建 `AppStore` 后保留动作位置、组次、已完成组和用户输入。
2. 用户计划 5 组时，第 4 组 RIR 0 仍保留第 5 组，不自动结束或减组。
3. 下组重量只受次数、RIR、恢复与历史影响；改变动作质量或旧版组后感受不改变建议值。
4. 下组建议可手动覆盖并立即持久化；完成后再增加计划组数会重新开放新组。
5. 组间休息覆盖热身组、复合/孤立动作、RIR 0–1、次数下降、低恢复与历史 e1RM，算法输出限定于 60–300 秒。
6. 动作目录 ID 与展示名唯一，无复合“A + B”动作；旧名称可迁移至明确的史密斯、固定器械或绳索类别。
7. 部分保存会生成训练历史但不推进计划轮转；放弃会移除草稿。
8. 回收站支持单条永久删除和清空，永久删除后不能恢复。

已在 iPhone 17 Pro（iOS 26.5）Simulator 完成界面验收：训练页显示醒目组次、动作与整场完成数；第 1 组 RIR 0 后显示可编辑的下组 15 kg 建议和 3–5 分钟休息范围，第 2 组仍可继续。强制结束进程再重开后，自动返回同一动作、保留已完成组，并按时间戳继续倒计时。主动退出弹窗明确显示“继续未完成训练 / 保存并结束 / 放弃训练”。回收站软删除后显示恢复、单条永久删除与清空按钮，测试记录已恢复。

真机已使用原 Bundle ID `com.codex.fittune` 覆盖安装并启动，未卸载 App、未清除数据容器；CoreDevice 确认设备端版本为 `0.6.0` / build `6`。锁屏与手势操作的真机最终触感验收留给用户在下次实际训练时完成。

- App 版本：`0.6.0`，build `6`。
- 模拟器截图：`Screenshots/FitTune-v0.6-resumable-rest-simulator.png`、`Screenshots/FitTune-v0.6-exit-options-simulator.png`。
# FitTune v1.0 最终验证记录（2026-07-22）

## 交付范围

- 主应用版本 `1.0.0 (10)`，Bundle ID 保持 `com.codex.fittune`，支持覆盖安装并迁移 v0.6 快照。
- iOS 主应用包含 Live Activity 扩展；Apple Watch 目标、WatchConnectivity 桥和 HealthKit 训练采集代码已编译。
- 实时来源保持单设备占用：Apple Watch 或标准 BLE 心率设备二选一；华为健康属于延迟导入，不占实时连接位。
- 所有传感器和权限缺失均降级为估算或不可用，不阻塞训练。

## 自动验证

- `swift test`：包含迁移、计划快照、动作库、恢复、热量、休息、e1RM、HealthKit 去重、实时来源、摘要与导出。
- 固定基准文件位于 `FitTuneTests/Fixtures/`，每份包含算法版本、依据、预期值和容差。
- iPhone Simulator Debug/Release 构建以及 `FitTuneWatch` watchsimulator SDK 目标构建分别执行。
- 项目文件通过 `plutil -lint`；源代码通过 `git diff --check`。

## 真机覆盖安装

- 设备：iPhone 15 Pro Max（iPhone16,2），CoreDevice 状态 `connected`。
- 使用 Apple Development 签名完成 Release 真机构建；Live Activity 扩展随主应用签名并通过嵌入验证。
- 通过 `devicectl device install app` 覆盖安装，未执行卸载；随后成功启动 `com.codex.fittune`。
- 设备安装清单确认：版本 `1.0.0`、构建 `10`。
- 首次真机启动发现 WatchConnectivity 状态回调会重复激活会话并导致栈溢出；现已将激活限定为初始化一次，加入可重入回调回归测试，重新覆盖安装后确认进程持续运行且未产生新的崩溃日志。

## 已知平台限制

- 当前 Mac 有 watchOS SDK，但未安装与 Xcode 匹配的 watchOS Simulator runtime。为保证 iPhone 包可构建和覆盖安装，Watch App 暂未嵌入 iOS 包；Watch 目标可独立编译。购买 Apple Watch 后，安装对应 Xcode watchOS 组件并恢复 `Embed Watch Content` 即可随 iPhone App 分发。
- FIT 3 是否能向 Apple 健康写入睡眠、静息心率、步数等内容由华为健康版本、地区和用户授权决定。FitTune 只读取实际到达 Apple 健康的数据。
- 真机的锁屏实时活动、后台定位/运动和健康权限仍需在目标 iPhone 上逐项确认；模拟器通过不能替代权限弹窗验收。

---
